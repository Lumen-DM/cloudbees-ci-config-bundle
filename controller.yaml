removeStrategy:
  rbac: SYNC
  items: NONE
items:
- kind: folder
  name: lumen-dm
  groups:
  - members:
      users:
      - dmasters1
      - dmasters1-admin
    roles:
    - name: browse
      grantedAt: current
    - name: workshop-admin
      grantedAt: child
    name: Team Administrators
  filteredRoles:
  - workshop-admin
  - browse
  items:
  - kind: managedController
    name: controller
    properties:
    - healthReporting:
        enabled: true
    - configurationAsCode:
        bundle: lumen-dm-controller
    configuration:
      kubernetes:
        memory: 4000
        cpus: 1.0
        clusterEndpointId: default
        disk: 10
        storageClassName: premium-rwo
        domain: lumen-dm-controller
        namespace: controllers
        yaml: |
          kind: "StatefulSet"
          spec:
            template:
              metadata:
                labels:
                  networking/allow-internet-access: "true"
              spec:
                containers:
                - name: "jenkins"
                  env:
                  - name: "SECRETS"
                    value: "/var/jenkins_home/jcasc_secrets"
                  - name: "CONTROLLER_NAME"
                    value: "controller"
                  - name: "GITHUB_ORGANIZATION"
                    value: "Lumen-DM"
                  - name: "GITHUB_USER"
                    value: "dmasters1"
                  - name: "GITHUB_APP"
                    value: "cloudbees-ci-workshop"
                  - name: "CONTROLLER_SUBDOMAIN"
                    value: "lumen-dm-controller"
                  - name: "CASC_BUNDLE_ID"
                    value: "lumen-dm-controller"
                  - name: "AVAILABILITY_PATTERN"
                    value: "REPLACE_AVAILABILITY_PATTERN"
                  volumeMounts:
                  - name: "jcasc-secrets"
                    mountPath: "/var/jenkins_home/jcasc_secrets"
                volumes:
                - name: "jcasc-secrets"
                  secret:
                    secretName: cbci-mc-secret
